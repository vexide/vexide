[toolchain]
channel = "nightly-2026-03-07"
components = ["rust-src"]
# Rust doesn't have a prebuilt libstd for our target,
# so we can't specify the target key here to download one.
# Instead we use build-std.
